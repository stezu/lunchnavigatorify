extends layout

block content
    h1= title
    p Welcome to #{title}

    form(action='/', method='post')
        fieldset
            label(for='lunchspot') add a lunchspot
                input(id='lunchspot', name='lunchspot', type='text')
        div
            input(type='submit', value='submit')